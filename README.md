# Makeshift API for an academic project

Just put your MongoDB in a .env file in the root directory and run `npm start` to start the server on port 1337.

Just read the code for the rest, its simple anyway.